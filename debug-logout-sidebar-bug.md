# [OPEN] logout-sidebar-bug

## 症状
- 点击右上角 `退出登录` 后报错，没有回到登录页。
- 用户反馈当前页面里的对话列表异常或缺失。

## 预期
- 在对话页点击 `退出登录` 后，应该可靠回到登录页。
- 左侧应保留并正常显示对话列表。

## 假设
1. `portal` 服务未运行或端口不是 `3000`，导致退出登录跳转地址可达性错误。
2. 对话页右上角 `退出登录` 使用了 portal 地址，但当前环境并没有通过 portal 进入，因此没有有效桥接状态。
3. 我之前的 URL 清理或桥接参数处理影响了 OpenCode 启动状态，间接导致左侧对话列表状态异常。
4. 当前浏览器打开的并不是热更新后的源码页面，导致代码和实际界面不一致。
5. 左侧对话列表本身没有丢失，只是当前项目/会话状态没有被正确恢复，表现为“看起来没有列表”。

## 证据计划
- 检查本地 `3000/4444/4096` 端口服务是否实际在线。
- 检查 portal 登录跳转与 logout 目标 URL。
- 检查浏览器当前页面 DOM 和控制台报错。
- 检查左侧栏会话列表相关状态是否在运行时存在。

## 当前状态
- 仅进入证据采集阶段，未开始业务逻辑修复。
